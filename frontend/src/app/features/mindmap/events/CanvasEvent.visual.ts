import type { EventStrategy, CanvasEvent } from './EventStrategy';

export class VisualModeStrategy implements EventStrategy {
  handle(_event: CanvasEvent): void {
    // placeholder: implement visual mode behavior
  }
}

