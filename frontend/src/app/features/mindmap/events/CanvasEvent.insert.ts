import type { EventStrategy, CanvasEvent } from './EventStrategy';

export class InsertModeStrategy implements EventStrategy {
  handle(_event: CanvasEvent): void {
    // placeholder: implement insert mode behavior
  }
}

