import type { EventStrategy, CanvasEvent } from './EventStrategy';

export class NormalModeStrategy implements EventStrategy {
  handle(_event: CanvasEvent): void {
    // placeholder: implement normal mode behavior
  }
}

